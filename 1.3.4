#include <iostream> //ввод библиотеки

using namespace std; //импорт всего пространства имен std

int main() //главная функция main ()
{
    int g,v; //даются 2 целых числа
    cin >> g >> v; //ввод этих чисел 
    int t = g/v; //деление этих чисел,где t-целое число
    cout << t << endl; //вывод ответа
    ret
